home:
  build: ./test/home
  volumes:
    - .:/labs-ro:ro
  entrypoint: /labs-ro/test/home/entrypoint.sh
  links:
    - labs

labs:
  build: ./test/labs
  command: /usr/sbin/sshd -D -E /var/log/sshd.log
